# 前端 QA 报告

**状态：BLOCKED。** Phase A 固定提交 `3b3c270869663293769f99ab3cbc180904185a1f` 的旧 Gradio `run_dicom` API 已实际调用并与 HTTP 结果一致；这只证明旧单次调用入口，不证明新的真实 DICOM 页面。

截至本次 QA，`feat/p0-frontend-real` 工作区仍有未提交修改，远端没有该固定分支提交；按测试隔离要求未读取其临时实现作为正式依赖，也未对持续变化的页面宣布 PASS。

固定前端提交到达后执行：API 基础地址与服务端 Token 配置、401 错误提示和密钥不出现在浏览器响应/截图；上传状态和匿名 ID、单切片预测及 `LIVE_CASE` 来源、16/32/64 切换、112×80 原图与 224×224 响应图依据空间元数据叠加、`slice_id` 不混用、无效候选不绘制、Job 刷新恢复、网络中断和错误显示、Mock 显著区分、PLANNED/UNAVAILABLE 状态。14×14 比较网格只用于统计比较。需保存真实浏览器截图与可复现操作记录。

**当前结论：Frontend QA BLOCKED；没有可报告的新页面截图验收。**
