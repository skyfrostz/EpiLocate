# 接口落地路线与负责人边界

本路线按当前仓库和 B 分支状态制定。A 负责冻结算法语义、模型和实验口径；B 负责 `gradio_service` 的 API/UI 与任务管理；前端同事负责页面及叠加显示；数据/QA 同事负责独立核验。负责人姓名与具体日期由团队确认，此处只约定职责，不代替任务分配。

| 阶段 | 工作项 | 当前状态 | 依赖与验收 |
| --- | --- | --- | --- |
| P0 | 保留 B 的 PNG/JPG Mock P0；集成到最新主线 | B 分支已实现，未集成主线 | 从最新主线独立集成 `gradio_service/`；旧 P0 端点与测试通过，复核服务/展示站点不丢失 |
| P0 | `capabilities`、统一 source/版本、错误与 synthetic 契约 | 设计文档已完成，代码未实现 | B 后端按 OpenAPI 实现；前端仅启用 available；合同 fixture 验证 |
| P0 | DICOM 单切片真实 Baseline adapter 与模型缓存 | 离线算法可复用，Web 未实现 | A 提供 checkpoint/hash、预处理与测试向量；B 接线；模型严格加载、无额外下载、同源输入概率一致 |
| P0 | DICOM 病例/切片/预览/质量检查 | DICOM 单片预处理已有，病例服务未实现 | B 开发受控上传、匿名 ID、资产授权；前端切片浏览与空间叠加；去标识检查与坐标测试 |
| P0 | 冻结 validation 聚合结果只读展示 | 结果已冻结，服务未实现 | A 提供只读结果白名单/QA/hash；B 接口；前端区分 FROZEN_VALIDATION 与 LIVE_CASE |
| P0 | 在线遮挡任务与三尺度图层 | 离线算法已验证，Web 未实现 | A 固定公式/网格/投影；B job/asset；前端图层对齐、候选无效状态；synthetic 和受控开发影像验证 |
| P1 | NIfTI 解析与三维几何 | 未实现 | A/B 明确轴向、affine 和尺寸；用 synthetic NIfTI 校验坐标，再开放上传 |
| P1 | Robust 模型与配对比较 | 模型未训练，接口预留 | A 冻结新方法并完成独立 QA；B 使用相同 `Prediction`/`OcclusionResult` 契约；同病例切片、同协议配对 |
| P1 | 逐级稳定粗定位 | 方法未冻结，接口预留 | A 冻结尺度递进与区域有效性规则；B 接 `FutureModule`，前端按 stage 展示，不先生成假区域 |
| P2 | LIME 精细化 | 未实现，接口预留 | A 冻结粗定位先验/搜索边界/贡献定义；B 接 job/result；前端展示贡献和边界；不启动现阶段计算 |
| P2 | 医生修正及临床解释 | 未实现，接口预留 | 伦理/角色授权和临床流程明确后，B 建追加式事件库；前端并列展示模型/医生/金标准，不自动训练 |
| P2 | 展示站点与算法服务联通 | `aid_site` 仅占位 | 主线维护者提供网关/认证/对象授权；B 暴露受控服务；前端接实际能力查询 |

每阶段出入口：协议与模型版本核对 → synthetic contract 验证 → 受控开发影像功能验收 → 独立 QA → 显式启用 capability。正式 test seal 仍由独立流程管理，接口开发不触发测试集推理。
